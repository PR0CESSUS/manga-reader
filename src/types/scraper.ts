export type ScraperPatternType = {
  match: string;
  chapters: string;
  title: string;
  url: string;
  image: string;
  imageUrl?: string;
  description?: string;
  next: string;
  first: string;
};

export type ScraperAction = {};
